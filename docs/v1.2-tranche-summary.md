# v1.2 First Tranche Summary

Date: 2026-07-01
Epic: 20W-v1.2-first-tranche
Gate status: v1.2 first tranche complete; Epic 2 diagnostic/export pass cleared local G5
security review and is ready for human G6 release approval.

## Implemented

- Added the hardened v1.2 LEM-01 through LEM-04 directives to runtime state.
- Updated the graph execution path so LEM-01 emits a clean engineering payload, LEM-02 emits
  exactly `lookahead_horizon` strategic layers, and LEM-03 emits a deterministic execution matrix.
- Added telemetry exposed in final run state:
  - `E_c`
  - `delta_a`
  - `S_d`
  - `pruning_percentage`
  - `autonomous_decisions`
  - `total_decisions`
- Added local SQLite persistence in `20w_memory.db` through `memory_store.py`.
- Added `runs` and `bias_vectors` tables, seeded baseline Neil Strategic Bias Profile vectors,
  and stored successful run telemetry after `/run`.
- Injected `bias_profile` into explicit `node_bias_profiles["LEM-01"]` and
  `node_bias_profiles["LEM-02"]` state before each graph run.
- Added ADR-0001 for the approved SQLite persistence and telemetry architecture checkpoint.

## Assumptions

- `pruning_percentage` is derived from graph nodes skipped during a run. A normal run prunes zero
  nodes; a radar-to-validator recalibration path prunes LEM-01 and LEM-02, producing `0.5`.
- `S_d` counts internal recalibration as an autonomous decision. Normal runs produce `0.0`; a
  recalibration path produces `1 / 2 = 0.5`.
- The database is local runtime state and is intentionally excluded from Git.
- Existing graph topology remains unchanged for this tranche.
- `/run` rejects empty prompts, negative variance thresholds, and lookahead horizons below `1`
  so the required telemetry formula can be applied directly.

## Verification Performed

- `python -m compileall main.py memory_store.py`
- FastAPI `/run` smoke test for the standard path:
  - returned HTTP 200
  - returned `E_c`, `delta_a`, `S_d`, `bias_profile`, `node_bias_profiles`,
    `agent_directives`, `strategic_layers`, `execution_matrix`, and `run_id`
  - generated three strategic layers for `lookahead_horizon = 3`
  - created SQLite tables and inserted a successful run
- FastAPI `/run` smoke test for the recalibration path:
  - returned HTTP 200
  - visited `LEM-04` then `LEM-03`
  - returned `pruning_percentage = 0.5`
  - returned `S_d = 0.5`
- FastAPI validation smoke test rejected `lookahead_horizon = 0` with HTTP 422.

## Open Questions

- G4 live end-user acceptance still needs the Conductor to exercise the running build.
- Future tranches should decide whether telemetry formulas need domain-calibrated inputs rather
  than deterministic runtime proxies.
- The repository currently has role files in `agents/`, but not the `agent-team/ORCHESTRATOR.md`,
  `agent-team/registry.json`, or `agent-team/contracts/handoff.schema.json` paths named in
  `AGENTS.md`.

## Epic 2 Diagnostic Pass Addendum

Date: 2026-07-01
Epic: Epic 2 - Cognitive Diagnostic Probe
Gate status: G0, G2, G3, G4, and G5 validated locally; G6 remains human-owned.

### Implemented

- Updated the cockpit export action from JSON `COPY EXPORT` to markdown `EXPORT RUN DATA`.
- Added a focus-resilient clipboard fallback so browser focus loss does not block telemetry
  extraction.
- Export now includes:
  - `E_c`
  - `delta_a`
  - `S_d`
  - `pruning_percentage`
  - `autonomous_decisions`
  - `total_decisions`
  - structural drift variables
  - backend checkpoint logs
  - explicit `[LEM-01]` through `[LEM-04]` diagnostic dialogue sections
  - supervisor payload, strategic layers, execution matrix, and live terminal trace
- Kept the export credential-safe: it records only whether an API key was configured, never the
  key value.

### Verification Performed

- `docker compose down`
- `docker compose up -d --build --force-recreate`
- `docker compose ps`
  - backend route confirmed as `0.0.0.0:8001->8001/tcp`
  - frontend route confirmed as `0.0.0.0:8080->80/tcp`
- `Invoke-RestMethod http://127.0.0.1:8001/health`
  - returned `ONLINE`
- Live browser diagnostic run at `http://127.0.0.1:8080/`
  - provider `gemini`
  - model `gemini-1.5-pro`
  - variance threshold `75`
  - lookahead horizon `4`
  - exact Epic 2 market/RACI problem vector
  - terminal printed `STATUS: SUCCESS`
- Clipboard export validation:
  - markdown length: `20244`
  - contained `E_c`, `delta_a`, `S_d`, `pruning_percentage`
  - contained `[LEM-01]`, `[LEM-02]`, `[LEM-03]`, `[LEM-04]`
  - contained `STATUS: SUCCESS`
  - contained no Gemini/OpenAI API-key-shaped secret literal

### Caveat

- No local Gemini key was available in the repo or environment during the diagnostic run, so the
  verified export correctly recorded `API Key Configured: false`.

## Epic 3 Preparation: Adversarial Mode Overrides

The next work should stay atomic and ordered:

### Tranche 3A - State Interception

- Primary file: `index.html`
- Add manual cockpit controls to simulate a LEM-04 Radar failure and force the recalibration path.
- Acceptance target: a live run can deliberately drive `S_d >= 0.5` and show the forced
  recalibration state without modifying backend scoring semantics in the browser.
- Governance note: if the UI needs a new backend override endpoint rather than using the existing
  `/run` contract, route through G1 before implementation.

### Tranche 3B - Persistence Expansion

- Primary files: `memory_store.py`, `main.py`
- Extend persistence to index custom prompt metadata and execution velocity tags beside baseline
  telemetry.
- Acceptance target: successful `/run` persistence stores prompt metadata and velocity tags while
  preserving existing `runs` and `bias_vectors` behavior.

### Tranche 3C - The Hardening Gate

- Primary files: `main.py`, `docker-compose.yml`, `index.html`
- Lock adversarial override controls behind a clean cryptographic or environment-variable
  validation check.
- Acceptance target: override controls are unavailable unless the validation gate is configured,
  and no secret value is exposed to the browser or telemetry export.
